# 게시 검토 · 2026-09-15

게시 대상은 [GitHub](https://github.com/hyunaeee/visioneye-lab), 정적 `web/` 폴더는 [Vercel 뷰어](https://visioneye-lab.vercel.app)다. Python 실행과 GPU 추론은 로컬에서 수행하며 웹은 기록을 재생한다.

이번 변경에는 검출기 3종·추적 설정 3종의 조건별 3회 요약·이벤트, 첫 반복의 프레임 좌표, 12개 분석 영상, 비교 표, 검수·재현 문서를 포함한다. 같은 두 원본의 고유 프레임 수는 602다. SAM 3.1은 공식 체크포인트 접근 거부로 미실행이며 성능 수치를 넣지 않았다.

모델 가중치·가상환경·외부 코드 checkout·캐시·인증정보·개발 PC 절대 경로는 게시 대상에서 제외한다. `.runtime/`과 `runs/`는 Git에서 제외하고 Python·실측 원자료 폴더는 Vercel 정적 배포에서 제외한다. `PUBLISH_FILES.json`은 Git blob 바이트 기준 상대 경로·크기·SHA-256 목록이며 목록 자체는 재귀 해시를 피하기 위해 제외한다.

검증 기록:

- 기존 집계·저장·시각화·입력 자동 테스트 41개 통과.
- 새 실행 코드의 Python/JSON 구문·CLI·경로·CPU 추적 계약 확인. 공개 복사본으로 새 환경 설치나 전체 GPU 재측정은 수행하지 않음.
- 실측 36개 요약과 표의 집계·ID·중앙값·범위 일치 확인.
- 이벤트 매칭 독립 경계 사례 6개와 완전탐색 기준 무작위 200개 통과.
- 12개 분석 영상의 원본 SHA·프레임 수·해상도·FPS 정합 확인.
- 브라우저에서 12개 조건 모두 4초 탐색 후 원본·분석 시각 일치, 1280×720 디코딩, 미디어 오류 없음 확인.
- 390px 모바일 영상 세로 배치와 비교 표 내부 스크롤 확인. 페이지 가로 넘침 없음.

기존 프로젝트 전체 라이선스는 새로 지정하지 않는다. 외부 모델·패키지는 [제3자 표기](THIRD_PARTY_NOTICES.md)와 [재현 문서](docs/EXPERIMENT_REPRODUCTION.md)를 따른다.
